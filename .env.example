# Copia questo file in .env.local e riempi i due valori.
# Su Supabase: Project Settings -> API.
# .env.local non finisce nel repo (vedi .gitignore).

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
